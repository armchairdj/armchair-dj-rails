# frozen_string_literal: true

# if memo.total == memo.success.length
#   logger.info "Published all #{memo[:total]} scheduled articles."
# else
#   logger.error "Could only publish #{memo[:success].length} of #{memo[:total]} scheduled articles. Failed articles: #{memo[:failure].map(&:id).join(', ')}"
# end
