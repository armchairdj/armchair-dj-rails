RSpec.shared_examples "a_public_controller" do
  context "concerns" do
    pending "seo paginatable"
  end

  context "included" do
    # Tested in individual controllers.
  end

  context "instance" do
    # Tested in individual controllers.
  end
end
