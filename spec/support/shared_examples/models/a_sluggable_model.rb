# frozen_string_literal: true

require "rails_helper"

RSpec.shared_examples "a_sluggable_model" do
  context "class" do
    pending "class"
  end

  context "included" do
    pending "included"
  end

  context "instance" do
    pending "instance"
  end
end
