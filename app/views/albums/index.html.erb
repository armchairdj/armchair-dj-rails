<%
  @headline = @title = "Albums"
%>

<table>
  <tbody>
    <% @albums.each do |album| %>
      <tr>
        <td><%= link_to album.title, album %></td>
        <td><%= album.artists.first.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>
