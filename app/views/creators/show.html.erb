<%
  @headline = @title = @creator.name
%>

<%= render partial: "shared/playback/creators", locals: {
  creators: @creator.groups.for_site,
  headline: "Groups"
} %>

<%= render partial: "shared/playback/creators", locals: {
  creators: @creator.members.for_site,
  headline: "Members"
} %>

<%= render partial: "shared/playback/creators", locals: {
  creators: @creator.colleagues.for_site,
  headline: "Colleagues"
} %>

<%= render partial: "shared/playback/creators", locals: {
  creators: @creator.personae.for_site,
  headline: "Personae"
} %>

<%= render partial: "shared/playback/roles_for_creator", locals: {
  roles: @creator.display_roles(for_site: true)
} %>

<%= render partial: "shared/playback/works", locals: {
  works: @creator.works.for_site
} %>

<%= render partial: "shared/playback/contributions", locals: {
  contributions: @creator.contributions.for_site,
  context:       :creator
} %>

<%= render partial: "shared/playback/posts", locals: {
  posts: @creator.posts.for_site
} %>

<%= render partial: "shared/playback/links", locals: {
  links: @creator.links
} %>
