<%
  @headline = @title = @creator.name
%>

<%= render partial: "shared/creators", locals: {
  creators: @creator.groups,
  headline: "Groups"
} %>

<%= render partial: "shared/creators", locals: {
  creators: @creator.members,
  headline: "Members"
} %>

<%= render partial: "shared/creators", locals: {
  creators: @creator.colleagues,
  headline: "Colleagues"
} %>

<%= render partial: "shared/creators", locals: {
  creators: @creator.personae,
  headline: "Personae"
} %>

<%= render partial: "shared/media", locals: {
  media: @creator.display_media
} %>

<%= render partial: "shared/works", locals: {
  works: @creator.works.for_site
} %>

<%= render partial: "shared/contributions", locals: {
  contributions: @creator.contributions.for_site,
  context:       :creator
} %>

<%= render partial: "shared/posts", locals: {
  posts: @creator.posts.for_admin.reverse_cron
} %>
