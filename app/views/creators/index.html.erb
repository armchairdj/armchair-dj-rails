<%
  @headline = @title = "Creators"
%>

<% if @creators.empty? %>
  <%= content_tag(:p, "Nothing so far.") %>
<% else %>
  <%= content_tag(:dl, class: "horizontal right") do %>
    <%= render partial: "creators/creator", collection: @creators %>
  <% end %>

  <%= paginate @creators %>
<% end %>
