<%
  @headline = @title = "Creators"
%>

<% if @creators.any? %>
  <%= content_tag(:ul, class: "bulleted") do %>
    <% @creators.each do |creator| %>
      <%= content_tag(:li, link_to(creator.name, creator)) %>
    <% end %>
  <% end %>
<% end %>
