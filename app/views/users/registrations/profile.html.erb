<%
  @title    = [@user.username, "Profile"]
  @headline = @user.username
%>

<%# @user.display_name %>

<% if @user.bio.present? %>
  <%= content_tag(:section, class: "bio") do %>
    <%= paragraphs(@user.bio) %>
  <% end %>
<% end %>

<%= render partial: "shared/playback/articles", locals: {
  articles: @user.articles.for_site
} %>

<%= render partial: "shared/playback/works", locals: {
  works: @user.works.for_site
} %>

<%= render partial: "shared/playback/creators", locals: {
  creators: @user.creators.for_site
} %>

<%= render partial: "shared/playback/links", locals: {
  links: @user.links
} %>

