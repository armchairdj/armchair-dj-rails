<%
  @title = "Latest Posts"
%>

<% if @posts.empty? %>
  <%= content_tag(:p, "Nothing so far.") %>
<% else %>
  <%= content_tag(:dl) do %>
    <%= render partial: "shared/playback/post", collection: @posts %>
  <% end %>

  <%= paginate @posts %>
<% end %>
