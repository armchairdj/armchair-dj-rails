<%
  @headline = @title = "Posts"
%>

<% if @posts.empty? %>
  <%= content_tag(:p, "Nothing so far.") %>
<% else %>
  <%= content_tag(:dl) do %>
    <%= render partial: "posts/post", collection: @posts %>
  <% end %>

  <%= paginate @posts %>
<% end %>
