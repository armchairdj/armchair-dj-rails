<%
  @headline = @title = "Articles"
%>

<% if @articles.empty? %>
  <%= content_tag(:p, "Nothing so far.") %>
<% else %>
  <%= render partial: "posts/card", collection: @articles, as: :post %>

  <%= paginate @articles %>
<% end %>
