<%
  @headline = @title = "Articles"
%>

<% if @articles.empty? %>
  <%= content_tag(:p, "Nothing so far.") %>
<% else %>
  <%= content_tag(:dl) do %>
    <%= render partial: "shared/playback/post", collection: @articles %>
  <% end %>

  <%= paginate @articles %>
<% end %>
