<%
  @title = "All Posts"
%>

<% if @posts.empty? %>
  <%= content_tag(:p, "Nothing so far.") %>
<% else %>
  <%= render partial: "posts/card", collection: @posts, as: :post %>

  <%= paginate @posts %>
<% end %>
