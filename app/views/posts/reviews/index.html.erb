<%
  @title = "Reviews"
%>

<% if @reviews.empty? %>
  <%= content_tag(:p, "Nothing so far.") %>
<% else %>
  <%= content_tag(:dl) do %>
    <%= render partial: "posts/card", collection: @reviews, as: :post %>
  <% end %>

  <%= paginate @reviews %>
<% end %>
