<%= content_tag(:nav, id: "navbar") do %>
  <%= content_tag(:div, class: "constrain") do %>
    <%= content_tag(:ul) do %>
      <%= content_tag(:li, link_to("All Posts", posts_path   ), class: ("current" if @section == :all     )) %>
      <%= content_tag(:li, link_to("Articles",  articles_path), class: ("current" if @section == :articles)) %>
      <%= content_tag(:li, link_to("Mixtapes", mixtapes_path), class: ("current" if @section == :mixtapes)) %>
      <%= content_tag(:li, link_to("Reviews",  reviews_path ), class: ("current" if @section == :reviews )) %>
    <% end %>
  <% end %>
<% end %>
