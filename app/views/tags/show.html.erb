<%
  @headline = @title = @tag.display_name
%>

<%= render partial: "shared/playback/works", locals: {
  works: @tag.works.for_site
} %>

<%= render partial: "shared/playback/posts", locals: {
  posts: @tag.all_posts.for_site
} %>
