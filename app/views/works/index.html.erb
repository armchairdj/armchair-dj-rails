<%
  @headline = @title = "Works"
%>

<% if @works.empty? %>
  <%= content_tag(:p, "Nothing so far.") %>
<% else %>
  <%= content_tag(:dl, class: "horizontal right") do %>
    <%= render partial: "works/work", collection: @works %>
  <% end %>

  <%= paginate @works %>
<% end %>
