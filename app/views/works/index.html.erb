<%
  @headline = @title = "Works"
%>

<% if @works.any? %>
  <%= content_tag(:ul, class: "bulleted") do %>
    <% @works.each do |work| %>
      <%= content_tag(:li, link_to_work(work)) %>
    <% end %>
  <% end %>
<% end %>
