<%
  rel = page.next? ? "next" : page.prev? ? "prev" : nil
-%>

<li class="page<%= "-current" if page.current? %>">
  <% if page.current? %>
    <span><%= page %></span>
  <% else %>
    <%= link_to_unless page.current?, page, url, remote: remote, rel: rel %>
  <% end %>
</li>