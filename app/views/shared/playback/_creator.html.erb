<% parent = :ul if local_assigns[:parent].nil? %>

<% if parent == :ul %>
  <%= content_tag(:li, link_to_creator(creator)) %>
<% else %>
  <%= content_tag(:dt, link_to_creator(creator)) %>
  <%= content_tag(:dd, creator.display_roles(for_site: true).keys.join(", ")) %>
<% end %>
