<%
  @title = "Roles"
%>

<%= render "admin/shared/header", action: :index,
  prehead:  total_count_for(@roles),
  headline: @title
%>

<%= render "admin/shared/index",
  scopes:  @scopes,
  sorts:   @sorts,
  records: @roles,
  columns: [
    { name: "ID",     method: :id             },
    { name: "Medium", method: :display_medium },
    { name: "Name",   method: :name           },
  ]
%>
