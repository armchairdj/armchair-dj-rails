<%= simple_form_for([:admin, @role], html: { class: "admin"}) do |f| %>
  <%= f.error_notification %>

  <%= form_inputs do %>
    <%= adj_module do %>
      <%= content_tag(:fieldset) do %>
        <%= f.input :medium,
          as:            :select,
          collection:    @media,
          required:      true,
          include_blank: :translate,
          input_html:    { "data-controller": "selectable" }
        %>

        <%= f.input :name %>
      <% end %>
    <% end %>
  <% end %>

  <%= button_group submit_button(@role) %>
<% end %>
