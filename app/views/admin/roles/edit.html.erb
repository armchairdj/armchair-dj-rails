<%
  @title = ["Update Role", @role.name]
%>

<%= admin_header(@model_class, :edit,
  h1:       @title.first,
  instance: @role,
) %>

<%= render partial: "form", locals: {
  role:  @role,
  media: @media,
} %>
