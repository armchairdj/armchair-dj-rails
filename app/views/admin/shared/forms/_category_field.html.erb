<%= f.input Work.permitted_tag_param(category),
  as:         :select,
  collection: available_tags,
  label:      category.display_name,
  input_html: {
    "data-controller":             "create-tag",
    "data-create-tag-url":         admin_tags_path,
    "data-create-tag-category-id": category.id,
    multiple:                      category.allow_multiple?
  }
%>
