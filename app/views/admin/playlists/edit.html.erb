<%
  @title = [
    "Update Playlist",
    @playlist.display_author,
    @playlist.title
  ]
%>

<%= admin_header(@model_class, :edit,
  h1:       @title.first,
  instance: @playlist,
) %>

<%= render partial: "form" %>