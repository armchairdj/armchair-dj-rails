<%
  @title = [
    "Update Playlist",
    @playlist.display_author,
    @playlist.title
  ]
%>

<%= render "admin/shared/header", action: :edit, instance: @playlist,
  headline: @title.first
%>

<%= render "form" %>