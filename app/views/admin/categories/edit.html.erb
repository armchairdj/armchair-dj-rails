<%
  @title = ["Update Category", @category.name]
%>

<%= admin_header(@model_class, :edit,
  h1:       @title.first,
  instance: @category,
) %>

<%= render partial: "form", locals: {
  category: @category
} %>
