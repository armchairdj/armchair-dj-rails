<%
  @title = ["Update Medium", @medium.name]
%>

<%= admin_header(@model_class, :edit,
  h1:       @title.first,
  instance: @medium,
) %>

<%= render partial: "form", locals: {
  medium:     @medium,
  categories: @categories
} %>
