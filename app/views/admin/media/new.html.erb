<%
  @title = "Create Medium"
%>

<%= render partial: "admin/shared/admin_header", locals: {
  action:   :new,
  headline: @title
} %>

<%= render partial: "medium_form", locals: {
  medium:     @medium,
  categories: @categories
} %>
