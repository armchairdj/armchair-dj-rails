<%
  @title = ["Update Article", article_title(@article)]

  deck = if @article.published?
    "This post has already been published, so changes won't be autosaved."
  else
    "This post will be autosaved as you write and edit. When the screen flashes, you'll know it's been saved."
  end
%>

<%= render "admin/shared/header", action: :edit, instance: @article,
  headline: @title.first,
  deck:     deck
%>

<%= render "admin/posts/tabs", selected_tab: :edit do %>
  <%= render "form" %>
<% end %>
