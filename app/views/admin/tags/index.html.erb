<%
  @title = "Tags"
%>

<%= admin_header(@model_class, :index,
  h4: total_count_for(@tags),
  h1: @title
) %>

<%= render "shared/admin/index",
  scopes:  @scopes,
  sorts:   @sorts,
  records: @tags,
  columns: [
    { name: "ID",   method: :id   },
    { name: "Name", method: :name },
  ]
%>
