<%
  @title = "Create Tag"
%>

<%= render partial: "admin/shared/admin_header", locals: {
  action:   :new,
  headline: @title
} %>

<%= render partial: "form", locals: {
  tag:        @tag,
  categories: @categories
} %>
