<%
  @title = ["Update User", @user.display_name]
%>

<%= admin_header(@model_class, :edit,
  h1:       @title.first,
  instance: @user,
) %>

<%= render partial: "form", locals: {
  user:  @user,
  roles: @roles
} %>
