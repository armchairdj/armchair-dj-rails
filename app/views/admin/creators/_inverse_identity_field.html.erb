<%= f.simple_fields_for :pseudonym_identities, identity do |ff| %>
  <%= content_tag(:fieldset, class: "accepts-nested") do %>
    <%= ff.input :creator_id,
      as:            :select,
      label:         false,
      collection:    creators,
      required:      false,
      include_blank: :translate,
      input_html:    {
        "data-controller":           "create-real-name",
        "data-create-real-name-url": admin_creators_path,
      }
    %>

    <%= ff.error :pseudonym_id %>

    <%= render(partial: "shared/custom_checkbox", locals: {
      f:    ff,
      name: :_destroy
    }) unless ff.object.new_record? %>
  <% end %>
<% end %>
