<%= f.input :medium, as: :select,
  collection: Work.media_options,
  input_html: { "data-controller": "selectize" }
%>

<%= f.input :title %>

<%= render partial: "admin/works/work_fields_contributions", locals: {
  f:        f,
  creators: creators,
  roles:    roles
} %>
