<%
  @title = "Create Work"
%>

<%= admin_header(@model_class, :new,
  h1: @title
) %>

<% if @work.type.blank? %>
  <%= render partial: "form_medium_only" %>
<% else %>
  <%= render partial: "form" %>
<% end %>