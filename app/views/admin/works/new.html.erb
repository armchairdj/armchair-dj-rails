<%
  @title = "Create Work"
%>

<%= render partial: "admin/shared/admin_header", locals: {
  action:   :new,
  headline: @title
} %>

<%= render partial: "form", locals: {
  work:       @work,
  media:      @media,
  roles:      @roles,
  creators:   @creators,
  categories: @categories,
} %>
