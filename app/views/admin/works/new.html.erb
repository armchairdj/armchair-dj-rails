<%
  @title = 'New Work'
%>

<%= content_tag(:header, class: 'crud') do %>
  <%= content_tag(:h1, @title) %>
<% end %>

<%= render partial: 'work_form', locals: {
  work:     @work,
  roles:    @roles,
  creators: @creators
} %>
