<%
  @title = [
    "Update #{@work.model_name.human}",
    @work.credited_artists,
    @work.title
  ]
%>

<%= admin_header(@model_class, :edit,
  h1:       @title.first,
  instance: @work,
) %>

<%= render partial: "form" %>