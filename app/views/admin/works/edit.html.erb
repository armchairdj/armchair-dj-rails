<%
  @title = [
    "Update #{@work.medium.name}",
    @work.credited_artists,
    @work.title
  ]
%>

<%= admin_header(@model_class, :edit,
  h1:       @title.first,
  instance: @work,
) %>

<%= render partial: "form", locals: {
  work:       @work,
  creators:   @creators,
  types:      @types,
  roles:      @roles,
  works:      @works,
} %>
