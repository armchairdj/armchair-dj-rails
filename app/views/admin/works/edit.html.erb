<%
  @title = [
    "Update #{@work.true_human_model_name}",
    @work.display_makers,
    @work.title
  ]
%>

<%= admin_header(@model_class, :edit,
  h1:       @title.first,
  instance: @work,
) %>

<%= render partial: "form" %>