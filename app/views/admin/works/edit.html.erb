<%
  @title = [
    "Update #{@work.display_medium}",
    @work.display_makers,
    @work.title
  ]
%>

<%= render "admin/shared/header", action: :edit, instance: @work,
  headline: @title.first
%>

<%= render "form" %>